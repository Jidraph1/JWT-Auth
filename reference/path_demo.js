const path = require('path')

// Base file name
// console.log(path.basename(__filename))
// This gives you the current filename path

// Directory Name
// console.log(path.dirname(__filename))
// returns the Directory Name

// FileEXtension
// console.log(path.extname(__filename))
// returns the extension name in this case, JS

// create path Object
// console.log(path.parse(__filename))
// returns the rootCertificates, directory, base, extensiin and directory name